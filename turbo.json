{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "clean": {
      "cache": false
    },
    "build:elm": {
      "inputs": ["elm.json", "src/**/*.elm"],
      "outputs": ["dist/elm.js"]
    }
  }
}
