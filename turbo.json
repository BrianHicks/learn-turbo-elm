{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "clean": {
      "cache": false
    },
    "build": {
      "dependsOn": ["build:elm"]
    },
    "build:elm": {
      "inputs": ["elm.json", "src/**/*.elm"],
      "outputs": ["dist/elm.js"]
    },
    "test": {
      "dependsOn": ["test:elm"]
    },
    "test:elm": {
      "inputs": ["elm.json", "src/**/*.elm", "tests/**/*.elm"],
      "env": ["ELM_TEST_SEED"]
    }
  }
}
